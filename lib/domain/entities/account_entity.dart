/// AccountEntity
/// --
/// Representa a responta do método de *Login*, sendo o atributo
/// **[token]** o seu retorno.
///
class AccountEntity {
  final String token;

  AccountEntity(this.token);
}
